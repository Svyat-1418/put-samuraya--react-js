let initialState = {
    friends: [
        {id: 2, name: "Victoria"},
        {id: 3, name: "Valery"},
        {id: 4, name: "Pavel"}
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;